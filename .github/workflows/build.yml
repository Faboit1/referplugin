name: Build ReferPlugin

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build Paper plugin
        run: mvn --batch-mode --no-transfer-progress clean package -DskipTests

      - name: Build Velocity companion plugin
        working-directory: velocity-plugin
        run: mvn --batch-mode --no-transfer-progress clean package -DskipTests

      - name: Upload Paper JAR
        uses: actions/upload-artifact@v4
        with:
          name: ReferPlugin-${{ github.sha }}
          path: target/referplugin-*.jar
          if-no-files-found: error

      - name: Upload Velocity JAR
        uses: actions/upload-artifact@v4
        with:
          name: ReferVelocity-${{ github.sha }}
          path: velocity-plugin/target/refervelocity-*.jar
          if-no-files-found: error
